{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-sm-8 blog-main">

            <h3 class="text-center">{{ page.title }}</h3>

            <div>
                {{ page.body }}
            </div>

        </div>
    </div>
{% endblock %}